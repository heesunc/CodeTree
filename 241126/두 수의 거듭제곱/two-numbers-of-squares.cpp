#include <iostream>
#include <cmath>  // pow 함수 사용을 위한 헤더 파일
using namespace std;


int main() {
    // 여기에 코드를 작성해주세요.
    int a,b; cin >> a>>b;
    int result = pow(a,b);
    cout << result;
    return 0;
}